# index update QA

Goal: given indexable files, make sure the update is within plausibilty bounds

Indexable file is generated with `CrossrefExport`, we may generate a add-on
file that contains ISIL count.

Other option: make extraction faster, maybe use `jg`

